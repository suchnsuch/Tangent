import type { QueryResult } from 'common/indexing/queryResults';
import LinesBuilder from 'common/typewriterUtils/LinesBuilder';
import type { Line } from 'typewriter-editor';

export function queryToTypewriter(queryResult: QueryResult): Line[] {
	const builder = new LinesBuilder()

	
	
	return builder.lines
}
