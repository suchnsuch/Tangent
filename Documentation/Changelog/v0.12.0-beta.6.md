- _Actually_ fixed the checkbox regression. Checkboxes can now be changed to any state without affecting siblings. Adding a checkbox when one did not exist will still add checkboxes to lower siblings.